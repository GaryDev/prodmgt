@model ProductMgt.ViewModel.LoginViewModel

@{
    ViewBag.Title = "Product Management: Log in";
}

<h1>Login</h1>
<p>Please log in to access administrative area:</p>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    @Html.EditorForModel()
    <p><input type="submit" value="Log in" /></p>
}